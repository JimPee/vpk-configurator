export function getFeedback(state) {
  return state.order.feedback;
}

export function getShowSimulate(state) {
  return state.order.showSimulate;
}

export function getShowCreate(state) {
  return state.order.showCreate;
}
